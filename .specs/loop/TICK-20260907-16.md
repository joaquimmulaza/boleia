# TICK-20260907-16 — PR tip eng-15 → main

MODE: local
STATUS: NEED_HUMAN (merge)

## Feito

1. `main` local actualizada (`git pull` → `4a6e338`)
2. Branch `cursor/merge-eng15-tip-to-main` = tip eng-15 + merge limpo de `origin/main`
3. Push + PR aberto: https://github.com/joaquimmulaza/boleia/pull/95
4. **Sem merge automático**

## Gaps reais (próximo tick / pós-merge)

1. **`VITE_PLATFORM_IBAN`** — placeholder em `.env.example`; falta valor real no Vercel/produção (não inventar IBAN)
2. **`perfis.is_admin`** — marcar conta(s) admin para `/admin/pagamentos`
3. **Migrações remotas em falta vs tip** — BD tem eng5/7/9/11/13; **não** lista eng14 (renovação), eng15 (saída parcial), eng16 (notify domínio) — aplicar após merge via Supabase MCP
4. **Versões de migração divergentes** — remoto usou timestamps `2026090622…` / nomes MCP; repo tip usa `2026090701…` etc. — validar que `schema_migrations` não tenta reaplicar SQL já existente (eng5/13)
5. **Validar suite** — `pnpm test` no PR antes do merge
6. **Canon docs** — `PROJECT.md` / loop skills podem estar só untracked no workspace; confirmar se entram noutro PR

HANDOFF:
FROM: orchestrator
TO: human
STATUS: NEED_HUMAN
ARTIFACT: .specs/loop/TICK-20260907-16.md
NEXT: Correr testes no PR #95; merge manual; tick gaps
MODE: local

# TICK-20260907-19 — AdminRoute race + simulação

MODE: local
STATUS: NEED_HUMAN (merge PR #97)

## Causa do teu redirect

`AuthContext` libertava `loading` antes do perfil. `AdminRoute` via `session` + `profile=null` e mandava para `/acordos` mesmo com `is_admin=true`.

## Fix

- `profileLoading` + AdminRoute espera perfil
- PR: https://github.com/joaquimmulaza/boleia/pull/97

## Simulação browser

1. Contas QA criadas (admin/pax/driver)
2. Seed pagamento `comprovativo_enviado` 35 000 Kz
3. Login admin → `/admin/pagamentos` OK
4. Aprovar → sucesso + BD `em_custodia`
5. Tab custódia mostra liquidação

## UX notas

- UI admin clara (tabs, Aprovar/Rejeitar, valores Kz)
- BottomBar de passageiro ainda visível no admin (aceitável MVP; polish futuro)
- «Ver comprovativo» precisa de ficheiro real no Storage (seed usou path fake)

HANDOFF:
FROM: orchestrator
TO: human
STATUS: NEED_HUMAN
NEXT: Merge #97; abrir de novo `/admin/pagamentos` com a tua conta
MODE: local

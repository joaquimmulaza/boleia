# TICK-20260907-15 — tick local (abort rework)

MODE: local
STATUS: NEED_HUMAN

## Sync check

- `main` local **behind 20** vs `origin/main`
- Branch tick `cursor/iban-semi-escrow-tick` alinhada a `origin/main`
- **Retrabalho evitado:** pacote IBAN/escrow já implementado em cadeia cloud, **não** em `main`

## Já existe (remoto, fora de main)

- Branch: `origin/cursor/pacote-eng-5-pagamentos-escrow-0e89` (+ tip mais completa `pacote-eng-15-saida-parcial-5ff2`)
- Código: `PaymentService.js`, `AcordoPagamentoPanel.jsx`, `AdminPagamentos.jsx`, utils, specs, migrations
- BD projecto `boleia`: `pagamentos_acordo` (estados + comprovativo + liquidação)

## Acção orquestrador

**Não** despachar Shipwright/implementer para recriar iban-semi-escrow.

Pedir humano:

1. `git checkout main && git pull origin main` (atualizar main local)
2. Abrir/rever PR da cadeia eng → `main` (começar por eng-5 ou tip eng-15; cuidado com histórico paralelo ahead/behind)
3. Próximo `tick local` só depois do merge (ou para gaps reais)

HANDOFF:
FROM: orchestrator
TO: human
STATUS: NEED_HUMAN
ARTIFACT: .specs/loop/TICK-20260907-15.md
NEXT: Decidir merge da cadeia pacote-eng-* para main; não reimplementar pagamentos
MODE: local

# TICK-20260907-17 — align eng14–16 remote

MODE: local
STATUS: NEED_HUMAN (admin UUID + merge PR fix)

## Feito

1. Confirmado PR #95 merged em `main` (`101beef`); IBAN env (user) ✅
2. Aplicado remoto: eng15, eng16, eng14 (`renew_agreement_period`, `notify_domain_event`, índice `pagamentos_acordo_passageiro_mes_uniq`)
3. Fix eng14 SQL: `entity_id` → `subject_id`
4. Fix `PaymentService.test.js` mock multi-mês
5. Testes 47/47 verdes; **VERDICT: APPROVE** ([reviewer](bcac9634-cb12-4213-8e3a-7940abe02aa1))
6. Branch `cursor/tick-eng14-16-remote-align` + PR aberto

## Gaps restantes

1. `UPDATE perfis SET is_admin = true WHERE id = '<uuid>';` — precisa do teu UUID
2. Merge do PR de fix (humano)
3. Opcional: docs canon / loop skills

HANDOFF:
FROM: orchestrator
TO: human
STATUS: NEED_HUMAN
ARTIFACT: .specs/loop/TICK-20260907-17.md
NEXT: Merge PR; fornecer UUID admin ou correr SQL
MODE: local

# TICK-20260907-18 — admin access (sem UI)

MODE: local
STATUS: DONE (aguarda teste humano)

## Decisão

Fundador pediu **sem** link admin na UI (só ele acede). Mantemos só `AdminRoute` + `perfis.is_admin`.

## Feito

- `is_admin=true` em:
  - `joaquimmulazadev@gmail.com` (Cotarco Tecnologias)
  - `joaquimmulazadev2@gmail.com` (Joaquim João José Mulaza)
- Rota já registada em `App.jsx`: `/admin/pagamentos`
- **Não** adicionado link em Perfil / BottomBar

## Como testar

1. `git checkout main && git pull` (ou deploy Vercel actualizado)
2. Login com uma das contas acima
3. Abrir directamente: `/admin/pagamentos`
4. Se redireccionar para `/acordos`: refresh completo (perfil em cache) ou logout/login

HANDOFF:
FROM: orchestrator
TO: human
STATUS: DONE
ARTIFACT: .specs/loop/TICK-20260907-18.md
NEXT: Testar URL admin nas contas promovidas
MODE: local

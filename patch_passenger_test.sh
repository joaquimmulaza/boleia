sed -i 's/await new Promise((r) => setTimeout(r, 100));/await new Promise((r) => setTimeout(r, 100));\n      if (maplibregl.default.Map.mock.results.length > 0) {\n        const mapInstance = maplibregl.default.Map.mock.results[0].value;\n        const loadCall = mapInstance.on.mock.calls.find(call => call[0] === "load");\n        if (loadCall) loadCall[1]();\n      }/' src/pages/PassengerDashboard.test.jsx

# Forçar o pnpm a hoistá-los para node_modules/ de forma que o Rolldown
# os consiga resolver ao compilar o Service Worker customizado (src/sw.js)
public-hoist-pattern[]=workbox-*

# Permitir que o pnpm limpe node_modules sem pedir confirmação interativa (não-TTY/CI)
confirmModulesPurge=false
